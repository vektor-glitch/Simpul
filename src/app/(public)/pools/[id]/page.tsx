export default function PoolDetailPage({ params }: { params: { id: string } }) {
    return (
        <div className="min-h-screen flex items-center justify-center bg-gray-50">
            <div className="text-center">
                <h1 className="text-2xl font-bold text-gray-900 mb-2">Detail Pool</h1>
                <p className="text-gray-500">Halaman detail untuk pool belum tersedia dalam versi ini.</p>
            </div>
        </div>
    );
}
